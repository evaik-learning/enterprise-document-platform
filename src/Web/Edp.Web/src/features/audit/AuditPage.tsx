export function AuditPage() {
  return (
    <section>
      <h2 className="text-2xl font-semibold">Audit</h2>
      <div className="mt-4 rounded border border-slate-200 bg-white p-5 shadow-sm">
        Audit events will appear here as the audit service is added.
      </div>
    </section>
  );
}
