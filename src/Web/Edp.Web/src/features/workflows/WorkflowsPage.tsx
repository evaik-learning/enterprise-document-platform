export function WorkflowsPage() {
  return (
    <section>
      <h2 className="text-2xl font-semibold">Workflows</h2>
      <div className="mt-4 rounded border border-slate-200 bg-white p-5 shadow-sm">
        Workflow orchestration will appear here as the workflow service is added.
      </div>
    </section>
  );
}
